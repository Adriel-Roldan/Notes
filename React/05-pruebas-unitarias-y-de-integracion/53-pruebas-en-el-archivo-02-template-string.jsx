// Si nosotros queremos hacer una prueba de algo en otro archivo, entonces tenemos que exportar esta funcion o lo que vayamos a querer hacerle pruebas.

// Tenemos que ejecutar las pruebas en el archivo que estamos realizando las pruebas. Para esto en la consola de testing tenemos que apretar la W y luego la P para filtrar el archivo de pruebas dandole como nombre una referencia al archivo solicitado. 

// Tenemos que tener cuidado al construyendo las cosas porque en la prueba se ejecuta el archivo al que le estamos haciendo pruebas. 